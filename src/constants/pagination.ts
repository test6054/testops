/** mark-vue 列表默认每页条数；与后端 QueryDto.pageSize 默认、UiDataTable 默认保持一致。 */
export const DEFAULT_LIST_PAGE_SIZE = 10

/** 下拉远程搜索首屏条数（Selector / 远程搜索，非主列表）。 */
export const REMOTE_SEARCH_PAGE_SIZE = 10

/** 导出 / 批量写入等必须拉全量时的分页步长。 */
export const EXPORT_PAGE_SIZE = 100
